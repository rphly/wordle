module buttons_controller (
    input clk,  // clock
    input rst,  // reset
    
    input aIn,
    output is_pressed,
    output out[16]
  ) {
  .clk(clk) {
    button a(.button_input(aIn));
   }
  
  always {
    if(a.out){
      out = 16d1;
      is_pressed = 1;
    }else{
      out = 16b0;
      is_pressed = 0;
    }
  }
}